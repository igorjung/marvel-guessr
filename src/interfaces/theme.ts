export default interface ITheme {
  text: {
    primary: string
    secondary: string
    tertiary: string
    button: string
    disabled: string
  }
  background: {
    primary: string
    secondary: string
  }
  button: {
    primary: string
    disabled: string
  }
  border: {
    primary: string
    secondary: string
  }
}